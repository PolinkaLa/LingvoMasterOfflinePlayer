function UnzipService () {

	/**
	 * unzip downloaded archive with course 
	 * @param  {string} pathArchive - path to dowloaded archive in the file system
	 * @return {array[string]} - list paths to the file
	 */
	this.unzip = function(pathArchive) {
		var listUrlFiles = [];
		return listUrlFiles;
	}
};