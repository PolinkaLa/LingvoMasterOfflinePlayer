function ContentService () {
	
	/**
	 * function to get list paths to the files of course in the file system
	 * @param  {number} idCourse - the unique number of the course
	 * @return {array[string]} - list paths to the file of course
	 */
	this.getListContent = function (idCourse) {
		var listUrlFiles = [];
		return listUrlFiles;
	}
};

